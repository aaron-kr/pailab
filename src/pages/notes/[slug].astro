---
// src/pages/notes/[slug].astro
import PostLayout from "@/layouts/PostLayout.astro";
import { getCollection, type CollectionEntry } from "astro:content";

export async function getStaticPaths() {
  const notes = (await getCollection("notes")).sort(
    (a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime()
  );
  return notes.map((note, i) => ({
    params: { slug: note.slug },
    props: {
      note,
      prevPost: i < notes.length - 1
        ? { title: notes[i + 1].data.title, slug: notes[i + 1].slug }
        : undefined,
      nextPost: i > 0
        ? { title: notes[i - 1].data.title, slug: notes[i - 1].slug }
        : undefined,
    },
  }));
}

interface Props {
  note: CollectionEntry<"notes">;
  prevPost?: { title: string; slug: string };
  nextPost?: { title: string; slug: string };
}
const { note, prevPost, nextPost } = Astro.props;
const { Content } = await note.render();
const { title, pubDate, tags, naver_url, lang: noteLang, summary } = note.data;
const lang = "en" as const;
---

<PostLayout
  title={title}
  description={summary}
  pubDate={pubDate}
  lang={lang}
  tags={tags}
  naverUrl={noteLang === "both" ? naver_url : undefined}
  prevPost={prevPost}
  nextPost={nextPost}
>
  <Content />
</PostLayout>
